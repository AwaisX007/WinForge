{
  "name": "launchpad",
  "version": "0.1.0",
  "description": "Your personal launchpad for a new PC. Install apps and presets in one click.",
  "scripts": {
    "dev": "tauri dev",
    "dev-frontend": "vite src --port 5173 --strictPort",
    "build": "tauri build",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "@tauri-apps/api": "^1.5.0",
    "vite": "^5.0.0"
  }
}
